[package]
name = "orbital-core"
version = "0.1.0"
edition.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
name = "orbital_core"
path = "src/lib.rs"

[dependencies]
rusqlite = "0.33"
